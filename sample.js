
const userName = 'Pepe'; // statement (sentencia o instrucción)
if (userName === 2) { // expression
    let internalName = 12;
    if (internalName === 2) {
        const name = 12;
        console.log(name);
    }
    console.log(internalName);
}
console.error(userName);
